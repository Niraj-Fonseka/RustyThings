
/*
 The main function runs first in an executable 
 Rust is an ahead of time compiled language - meaning a pre compiled binary can be run anywhere without re compiling 
*/
fn main(){

    //Rust style is to indent with 4 spaces not a tab
    println!("Hello World ! "); //println! is a macro , we can say that because its been called with ! 
}